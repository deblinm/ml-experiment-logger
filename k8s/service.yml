#Kind and metadata

apiVersion: v1
kind: Service
metadata:
  name: ml-experiment-logger-service

#Spec

spec:
  selector:
    app: ml-experiment-logger
  type: NodePort
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30000